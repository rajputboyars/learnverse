// Which real Jahia screenshots have been added under public/images/jahia/.
//
// Every lesson screenshot declares the path of its real capture (for example
// /images/jahia/content-editor/content-editor-title-field.png) and a drawn
// mockup of the same screen. Until the file is listed here, the mockup is
// shown and labelled "Illustration" — so the page never requests an image that
// does not exist, and never passes a drawing off as a capture.
//
// To add one: save the capture at the declared path, then add the path below.
// See public/images/jahia/README.md for the full list of expected files.

export const AVAILABLE_SCREENSHOTS = new Set([
  // '/images/jahia/jcontent/jcontent-media-library.png',
]);
